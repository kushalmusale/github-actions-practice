name: Python CI - Matrix Build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  tests:
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    strategy:
      matrix:
        python-version: [3.9.25, 3.10.13, 3.11.5]

    steps:
      - name: Pull code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run Tests
        run: pytest -v --maxfail=1

# name: Python CI - Matrix Build (With Cache)

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   tests:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10

#     strategy:
#       fail-fast: false
#       matrix:
#         python-version: ["3.9.18", "3.10.13", "3.11.8"]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v4
#         with:
#           python-version: ${{ matrix.python-version }}

#       # 🔹 PIP CACHE STEP (KEY ADDITION)
#       - name: Cache pip dependencies
#         uses: actions/cache@v3
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-${{ matrix.python-version }}-
#             ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           python -m pip install -r requirements.txt

#       - name: Run tests
#         run: pytest -v --maxfail=1

